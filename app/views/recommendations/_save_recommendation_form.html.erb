<%= simple_form_for recommendation,
  url: {controller: "answer", action: "save", recommendation_id: recommendation.id},
  method: :get, remote: true do |f| %>

  <%= f.submit "Save", class: "btn btn-primary btn-lg" %>
<% end %>
