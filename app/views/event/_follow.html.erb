<%= form_for(current_user.event_relationships
  .build(followed_id: event.id)) do |f| %>

  <div><%= hidden_field_tag :followed_id, event.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary btn-xs" %>
<% end %>
